name: Release

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: https://registry.npmjs.org

      - name: The version surfaces agree
        run: |
          version="$(node -p 'require("./package.json").version')"
          if [ "${{ github.event_name }}" != "workflow_dispatch" ]; then
            [ "$version" = "$GITHUB_REF_NAME" ] \
              || { echo "::error::package.json is $version, tag is $GITHUB_REF_NAME"; exit 1; }
          fi
          grep -qE "^## \[?${version//./\\.}\]? - " CHANGELOG.md \
            || { echo "::error::CHANGELOG.md has no dated section for $version"; exit 1; }
          repository="$(node -p 'require("./package.json").repository.url')"
          [ "$repository" = 'git+https://github.com/markup-carve/highlightjs-carve.git' ] \
            || { echo "::error::package.json repository.url cannot support npm provenance"; exit 1; }

      - name: The npm credential authenticates
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          [ -n "$NPM_TOKEN" ] \
            || { echo "::error::NPM_TOKEN is not configured"; exit 1; }
          code="$(curl -sS -o /tmp/npm-who.json -w '%{http_code}' \
            -H "Authorization: Bearer $NPM_TOKEN" \
            https://registry.npmjs.org/-/whoami)"
          [ "$code" = 200 ] \
            || { echo "::error::NPM_TOKEN does not authenticate (HTTP $code)"; exit 1; }
          echo "npm: authenticated as $(node -p 'require("/tmp/npm-who.json").username')"

      - run: npm install --no-audit --no-fund
      - run: npm test

      - name: The package contains the definition and its entry
        run: |
          tarball="$(npm pack --silent)"
          listing="$(tar -tzf "$tarball")"
          for file in index.js languages/carve.js; do
            printf '%s\n' "$listing" | grep -qx "package/src/$file" \
              || { echo "::error::$tarball is missing src/$file"; exit 1; }
          done

      - name: Publish to npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            npm publish --dry-run --provenance --access public
            echo "::notice::rehearsal - nothing was published"
          else
            npm publish --provenance --access public
          fi

      # A package on the registry with an empty releases page gives a reader no
      # notes and no obvious source for the version they installed.
      - name: Publish the matching GitHub release
        if: github.event_name != 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          notes="$(awk -v v="$GITHUB_REF_NAME" '
            $0 ~ "^## \\[?" v "\\]? - " { on = 1; next }
            on && /^## / { exit }
            on { print }
          ' CHANGELOG.md)"
          [ -n "$notes" ] || { echo "::error::no CHANGELOG section for $GITHUB_REF_NAME"; exit 1; }
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release edit "$GITHUB_REF_NAME" --draft=false
          else
            gh release create "$GITHUB_REF_NAME" --title "$GITHUB_REF_NAME" --notes "$notes"
          fi
          gh api "repos/$GITHUB_REPOSITORY/releases/tags/$GITHUB_REF_NAME" \
            --jq 'if .draft then error("release is still a draft") else "published \(.tag_name)" end'
